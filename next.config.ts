/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // keep this only if you use the new app directory
  },
}

export default nextConfig
